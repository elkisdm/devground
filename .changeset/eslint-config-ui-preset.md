---
"@devground/eslint-config": minor
---

Add an opt-in `./ui` preset: `jsx-a11y` accessibility rules plus parametrizable warn-level
restrictions for native primitives (`ownComponents`) and icon imports (`iconLayer` +
`restrictedIconPackages`).
