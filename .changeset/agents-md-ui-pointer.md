---
"@devground/agents-md": patch
---

Add a pointer to `docs/ui-conventions.md` at the top of §9 (Semantic Tokens in UI), so
projects with their own UI conventions overlay take precedence over the generic rules.
