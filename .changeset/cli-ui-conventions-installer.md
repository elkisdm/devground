---
"devground-init": minor
---

Add the `ui-conventions` installer: installs `@devground/ui-conventions` and runs its bin,
only when a React/Next.js frontend is detected. Auto-skips (left untouched) on non-frontend
(Node/library) stacks.
