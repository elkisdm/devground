# Code map

Índice de *dónde vive cada cosa*. Lo mantiene `spec-flow` en cada cambio: el
índice acota la búsqueda, el código confirma. No es un manual de *cómo* (eso son
el código y los ADRs), sino de *dónde*.

| Subsistema | Paths | Responsabilidad | Entrypoints | ADRs |
|------------|-------|-----------------|-------------|------|
| **CLI (devground-init)** | `packages/cli/src/` | Scaffolding interactivo de los estándares devground en un proyecto | `src/index.ts` (bin `devground-init`); `src/select.ts` (routing de presets, puro/testeable); `src/detect-stack.ts` (detecta framework/PM y `hasSwift` por `Package.swift`/`.xcodeproj`, tolera repos sin package.json — ADR-0021); `src/installers/*.ts` (un installer por herramienta; `vitest.ts` scaffoldea `vitest.config.mjs` con ratchet autoUpdate + scripts test/test:coverage — ADR-0025); `src/installers/write-guard.ts` (no-sobreescribe); `src/utils/exec.ts` (`run` vía execSync), `utils/package-json.ts`; `ui-conventions.ts` instala `@devground/ui-conventions` solo en React/Next | 0001, 0010–0012, 0021, 0025 |
| **deepcheck** | `packages/deepcheck/` | Verificación profunda multi-agente (QA+Validación+Auditoría) con confirmación adversarial y destilación de skills por flujo. 13 dimensiones, incl. `aud-premortem` (generador de composición/supuestos rotos, eje ortogonal a las per-componente — destilado del red-team de `docs/reasoning-craft-playbook-vol2.md`) y `aud-ui-conventions` (UI generada vs base de `@devground/ui-conventions` + overlay del proyecto; regla de promoción overlay↔base; rastro parseable por key para dev-metrics futuro) | skill `skills/deepcheck/SKILL.md`; ejecutable `workflows/deepcheck.workflow.js` (corre vía la herramienta Workflow); dimensiones en `DIMENSIONS[]` sincronizadas con `references/roles.md`; bin `setup.js` (`devground-deepcheck`); ledgers en `audits/<flujo>/` | 0013 |
| **agents-md** | `packages/agents-md/` | Reglas de desarrollo con IA: crea `AGENTS.md` en la raíz y symlinks para Claude, Cursor, Copilot, Gemini; añade `.codex/` para OpenAI Codex. Además, 4 hooks curados del harness de Claude Code que hacen las reglas ejecutables al editar (prettier, typecheck single-file, aviso console.log, session-summary para dreaming) — inspirados en ECC (`research/ecc-inspiration.md` §2), kill switch `DEVGROUND_HOOKS_DISABLE` | bin `devground-agents` (`setup.js`); bin `devground-hooks` (`install-hooks.js`, respeta `hooks` existente en settings.json); `hooks/*.mjs` + `hooks/hooks.json` (tests de contrato en `hooks/*.test.mjs`); `AGENTS.md` (reglas compartidas); `PROMPT.md` (instrucciones portables a cualquier agente) | — |
| **Config presets** | `packages/{prettier,eslint,tsconfig,commitlint,lint-staged,husky,vitest}-config/` | Configuración compartida instalable. `vitest-config` exporta `CRITICAL_GLOBS`/`CRITICAL_THRESHOLDS` (umbral alto fijo en rutas dinero/leads/auth — ADR-0012); `husky-config` añade hook `pre-push` de aviso suave de cobertura (ADR-0025); `eslint-config` exporta preset `./ui` opt-in (jsx-a11y + no-restricted-syntax/imports parametrizables) | archivos estáticos publicables | 0003–0005, 0012, 0024, 0025 |
| **logger** | `packages/logger/` | Logger sin dependencias con niveles ANSI (header/info/success/warn/error); compartido por todos los paquetes devground | `src/index.ts` (export-only, sin bin) | — |
| **architecture-guide** | `packages/architecture-guide/` | Knowledge base + ADR templates instalables en cualquier proyecto | bin `setup.js`, `adr-new.js` | — |
| **dev-metrics** | `packages/dev-metrics/` | Serie temporal de métricas de codificar-con-agentes (git + transcripts) + impacto spec-flow | bin `dist/index.js` (`dev-metrics`); subcomando `spec-flow-impact` (`commands/spec-flow-impact.ts`); motor `lib/spec-flow-segment.ts` (segmentación per-commit, control recency-matched); `lib/spec-flow-events.ts` (parser JSONL); `lib/detectors.ts` (test/ADR estrictos, anti-substring; reconoce tests Swift: `Tests/` y `*Tests.swift`); `lib/spec-flow-report.ts`; subcomando `orientation` (`commands/orientation.ts`, motor `lib/orientation.ts` = tokens antes del 1er edit + payoff codemap) | 0006, 0014, 0015 |
| **sdd (spec-flow)** | `packages/sdd/` | Skill de intake SDD instalable (classify→tier→brief→telemetría); `skill/` = copia versionada de `~/.claude/skills/spec-flow/` | bin `setup.js` (`devground-sdd`, instala a `.claude/skills/spec-flow/`, `--global` a `~`) | 0016 |
| **design-taste** | `packages/design-taste/` | Skills de diseño UI/UX anti-slop instalables (layout/tipografía/motion/spacing); `skills/*` = 10 skills vendorizadas de `Leonxlnx/taste-skill` (MIT, SHA `b177427`): 5 de texto/diseño + 5 de imagen/Stitch (`stitch-design-taste` incluye `DESIGN.md`); atribución en `skills/ATTRIBUTION.md` | bin `setup.js` (`devground-design-taste`, copia el árbol `skills/*` a `.claude/skills/`, `--global` a `~`, copia guardada) | — |
| **ui-conventions** | `packages/ui-conventions/` | Skill que carga convenciones de UI (5 categorías + regional Chile es-CL) como contexto ANTES de generar/editar frontend; base universal + overlay por-proyecto (`docs/ui-conventions.md`, precedencia gana); SKILL §5 "maintain the overlay" (quien lo usa lo mantiene); §6 referencia `@devground/chile-formats` | bin `setup.js` (`devground-ui-conventions`, copia `skills/ui-conventions/` a `.claude/skills/`, `--global` a `~`); `references/{base,mining-prompt,overlay-template}.md` | 0023, 0024 |
| **chile-formats** | `packages/chile-formats/` | Helpers es-CL sin dependencias runtime: RUT (formateo puntos+guion, validación módulo 11, clean/DV), teléfono (visual +56 9 XXXX XXXX, normalize a +569XXXXXXXX), moneda/UF/número (Intl NumberFormat es-CL memoizado) | `src/{rut,phone,currency,index}.ts`; export-only sin bin (publica dist); tests vitest por módulo | 0024 |
| **agregador** | `packages/devground/` | Bundle de los 7 presets de config + `devground-setup` | bin de setup | — |
| **model-orchestrator** | `tools/model-orchestrator/` | Routing de modelos por complejidad tras `spec-flow`: asigna modelo+effort por tarea (Opus/Sonnet/Haiku), estima costo y despacha sub-agentes. Copia versionada; fuente activa en `~/.claude/skills/model-orchestrator/` | motor `skills/model-orchestrator/engine.mjs` (Node puro, invariantes en código, `selftest` 30/30); piso `policy.json`; tarifas `pricing.json`; juez `agents/model-router.md` (Haiku, ±1 nivel); inyección de guía por kind `references/kind-playbooks.md` (checklists destilados de `docs/*-playbook.md`, medido vía `playbook_injected` en decisions.jsonl) + checklist UI/FRONTEND inyectado aditivamente por superficie frontend (no un kind nuevo); patch `patches/spec-flow-step-3.5.md` | 0017 |
| **swift-foundation** (Swift) | `swift-foundation/` | Monorepo SPM fundacional ("librería de librerías") para apps iPhone en Swift — Fase 1. Grafo de módulos por capa, isolation por capa, core Swift puro portable. Consumidor, no paquete npm de devground | `Package.swift` (tools 6.2, `swiftLanguageModes: [.v6]`); `Sources/{FoundationUtils,Domain,Networking,Persistence,DesignSystem,FeatureInterfaces,BiometricAuth,FeatureAuth,AppFeature}/`; primer vertical **Auth con Face ID** (Domain: use case + protocolos; BiometricAuth: adaptador LocalAuthentication nonisolated; FeatureAuth: `AuthModel` @Observable + `AuthView`); `Tests/{DomainTests,FeatureAuthTests}/`; ADRs de dominio en `swift-foundation/docs/adr/`. Verificar: `swift build`; tests con toolchain Xcode (`DEVELOPER_DIR=…Xcode… xcrun swift test`) | 0018–0021 |
| **swift-tooling** (Swift) | `packages/swift-{format-config,package-template,design-tokens,ci}/` | Paquetes `@devground/swift-*` (Fases 2, 4, 5): config SwiftFormat+SwiftLint, plantilla `Package.swift` con isolation por capa, tokens semánticos + Liquid Glass helpers, y plantillas CI/CD (GitHub Actions + Fastlane Match). Artefactos copiables declarados en `index.json` (mapeo src→dest) para el instalador del CLI | `swift-format-config/config/*`; `swift-package-template/templates/Package.swift`; `swift-design-tokens/templates/*.swift`; `swift-ci/templates/{ci.yml,Fastfile,Matchfile}`; `index.json` (manifest) | 0019, 0020, 0021 |

> Investigación fundacional iOS/Swift + devground políglota: `research/ios-swift-engineering/`
> (`informe-fundacion.md`, `DECISIONS.md`).
>
> Skills destiladas por deepcheck viven en `.claude/skills/audit-<flujo>/` del
> proyecto auditado (ej: `.claude/skills/audit-devground-init/`).
>
> Skill `escritura-tecnica` (`.claude/skills/escritura-tecnica/`): checklists de
> narrativa/síntesis/enseñanza para redactar ADR/README/PR/guías, destilados de
> `docs/reasoning-craft-playbook*.md`. Cubre la escritura de docs NO orquestada
> (la orquestada ya la cubre el checklist DOCS de `kind-playbooks.md`).
