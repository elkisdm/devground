# deepcheck — auditoría de "devground-init" (Corrida piloto 1)

> Corrida: 2026-06-03 · 23 hallazgos confirmados · 8 descartados
> Nota: reconstruido del output de la corrida (el ledger automático no se escribió por el bug de plumbing de `args`, ya corregido).

## Resumen por rol

| Rol | Confirmados |
|-----|-------------|
| QA | 9 |
| Validación | 7 |
| Auditoría | 7 |

## Hallazgos confirmados

### QA — ¿funciona?
- **[HIGH] El instalador de lint-staged escribe un valor de string que lint-staged rechaza, dejando el camino feliz roto** (`qa-happy`)
  - `/Users/macbookpro/Documents/devground-1/packages/cli/src/installers/lint-staged.ts:11-13`
  - Evidencia: pkg['lint-staged'] = '@devground/lint-staged-config'; ops.writePackageJson(targetDir, pkg);  // escribe la clave como STRING en package.json
  - Por qué importa: El README declara que lint-staged queda 'configured'. Pero a diferencia de prettier (que SI soporta nativamente "prettier": "module-name" como referencia a un modulo), lint-staged NO resuelve un string como nombre de paquete. Verificado contra lint-staged@16.4.0: validateConfig.js linea 15 hace `if (!config || (typeof config !== 'object' && typeof config !== 'function'))` y lanza 'Configuration should be an object or a function'. Cuando lint-staged lee la clave 'lint-staged' de package.json y encuentra el string '@devground/lint-staged-config', falla la validacion: nunca lo importa como modulo. El index.js real del paquete (`module.exports = { '*.{ts,tsx}': [...] }`) jamas se carga. Resultado del camino feliz: el usuario corre `devground-init -y`, ve 'lint-staged configured' en verde, pero su pre-commit con lint-staged no funciona. La operacion no hace lo que dice. El instalador deberia escribir el objeto inline (require/import del config) o un archivo .lintstagedrc, no un string.
- **[HIGH] Los installers sobrescriben archivos de config preexistentes sin avisar ni respaldar** (`qa-edge`)
  - `/Users/macbookpro/Documents/devground-1/packages/cli/src/installers/ops.ts:20`
  - Evidencia: writeFile: (path, content) => writeFileSync(path, content, 'utf-8'), — usado por eslint.ts (escribe 'eslint.config.mjs'), commitlint.ts ('commitlint.config.js') y tsconfig.ts ('tsconfig.json'/'tsconfig.typecheck.json'). No hay ningún existsSync ni guard de sobrescritura en todo src/installers (grep de existsSync/overwrite/backup/already devuelve vacío).
  - Por qué importa: Caso de borde típico: el usuario corre devground-init sobre un proyecto que YA tiene tsconfig.json, eslint.config.mjs o commitlint.config.js. writeFileSync trunca el archivo y lo reemplaza por completo, sin prompt, sin diff, sin backup. Para un scaffolder que se promociona como instalable 'en cualquier proyecto', destruir silenciosamente la configuración existente es pérdida de datos del usuario.
- **[HIGH] El resumen final cuenta TODOS los installers como exitosos aunque alguno haya lanzado, y el exit code sigue siendo 0** (`qa-edge`)
  - `/Users/macbookpro/Documents/devground-1/packages/cli/src/index.ts:96-108`
  - Evidencia: for (const installer of selectedInstallers) { try { installer.install(options); } catch (err) { ... error(`Failed to install ${installer.name}: ${message}`); } } ... success(`${selectedInstallers.length} tool(s) configured successfully.`); — no existe ninguna variable de conteo de fallos (grep de failCount/results/push en index.ts devuelve vacío).
  - Por qué importa: Caso de borde: un sub-paso lanza (p.ej. husky.ts/agents-md.ts/architecture-guide.ts hacen ops.run('npx devground-...') que tira si el binario no existe o falla la red; addDevDependency hace execSync que tira en non-zero exit). El error se loguea, pero el bucle continúa y al final imprime 'N tool(s) configured successfully' con N = TODOS los seleccionados, incluyendo los que fallaron. Además process.exit nunca se invoca con código de error, así que en CI el comando reporta éxito (exit 0) pese a instalaciones fallidas. El conteo debería ser de los que efectivamente tuvieron éxito y el proceso debería salir distinto de 0 si hubo fallos.
- **[HIGH] Fallo de cualquier instalador no afecta el exit code: el CLI siempre sale 0 y reporta éxito** (`qa-errors`)
  - `/Users/macbookpro/Documents/devground-1/packages/cli/src/index.ts:96-108`
  - Evidencia: for (const installer of selectedInstallers) { try { installer.install(options); } catch (err) { ... error(`Failed to install ${installer.name}: ${message}`); } } ... success(`${selectedInstallers.length} tool(s) configured successfully.`);
  - Por qué importa: El catch por instalador solo loguea y continua; no se registra ningun contador de fallos ni se ajusta el codigo de salida. Si TODOS los instaladores fallan (p.ej. red caida en `npx`/`pnpm add`), el proceso igual termina con exit 0 e imprime 'N tool(s) configured successfully'. Esto rompe cualquier uso en CI/scripts: `devground-init && echo ok` siempre pasa. Ademas el mensaje de exito miente: cuenta los instaladores SELECCIONADOS, no los que realmente tuvieron exito.
- **[MEDIUM] prettier/lint-staged pisan claves existentes en package.json sin verificar conflicto** (`qa-edge`)
  - `/Users/macbookpro/Documents/devground-1/packages/cli/src/installers/prettier.ts:11-13`
  - Evidencia: const pkg = ops.readPackageJson(targetDir); pkg.prettier = '@devground/prettier-config'; ops.writePackageJson(targetDir, pkg); — idéntico en lint-staged.ts donde hace pkg['lint-staged'] = '@devground/lint-staged-config'.
  - Por qué importa: Si el package.json ya define una clave 'prettier' o 'lint-staged' (objeto de config inline o referencia a otro paquete), se reemplaza sin avisar. El usuario pierde su configuración previa de Prettier/lint-staged sin enterarse, porque el installer reporta success() igual.
- **[MEDIUM] Un --preset con valor inválido cae en silencio al modo interactivo en vez de fallar** (`qa-edge`)
  - `/Users/macbookpro/Documents/devground-1/packages/cli/src/index.ts:65-91`
  - Evidencia: if (opts.yes || opts.preset === 'full') {...} else if (opts.preset === 'agents-only') {...} else { const response = await prompts({ type: 'multiselect', ... }) }
  - Por qué importa: Un typo como --preset ful o --preset agents (o cualquier string no reconocido) no se valida: no coincide con 'full' ni 'agents-only', así que cae al else y abre el prompt interactivo. En un entorno no-TTY/CI el prompt no puede responderse y el flujo queda colgado o resuelve sin selección; en cualquier caso el usuario nunca recibe el mensaje de que su preset es inválido. Debería validarse el valor del preset y abortar con error explícito.
- **[MEDIUM] El catch de detectStack descarta el error real y emite un mensaje engañoso ante JSON inválido** (`qa-errors`)
  - `/Users/macbookpro/Documents/devground-1/packages/cli/src/index.ts:48-54`
  - Evidencia: try { stack = detectStack(targetDir); } catch { error('Could not read package.json in the current directory.'); error('Make sure you run devground-init from a project root.'); process.exit(1); }
  - Por qué importa: El `catch` sin binding (`catch {`) descarta el Error original. detectStack falla en dos casos distintos con la misma salida: (a) package.json ausente y (b) package.json presente pero con JSON malformado (JSON.parse en package-json.ts:12 lanza SyntaxError). En el caso (b) el mensaje 'Could not read package.json' es incorrecto y no accionable: el archivo SI se leyo, fallo el parseo, y el detalle del SyntaxError (posicion del error) se pierde por completo. El exit 1 es correcto, pero el diagnostico es opaco.
- **[LOW] El CLI reporta version 1.0.0 hardcodeada mientras el paquete publicado es 1.0.2** (`qa-happy`)
  - `/Users/macbookpro/Documents/devground-1/packages/cli/src/index.ts:39-43`
  - Evidencia: .version('1.0.0') y header('devground-init v1.0.0'); package.json declara "version": "1.0.2" y CHANGELOG.md confirma 1.0.2 publicada
  - Por qué importa: Camino feliz de los flags -V/--version y del banner inicial: el README los lista como funcionalidad ('-V, --version | Output version'). El usuario que ejecuta `devground-init --version` o ve el header obtiene '1.0.0', que no corresponde a la version real instalada (1.0.2). La version esta hardcodeada en tres literales en vez de leerse de package.json, por lo que diverge en cada release. Es una salida que no dice la verdad sobre lo que se esta ejecutando.
- **[LOW] ESLint escribe eslint.config.mjs sin detectar configuraciones rivales (.eslintrc, eslint.config.js)** (`qa-edge`)
  - `/Users/macbookpro/Documents/devground-1/packages/cli/src/installers/eslint.ts:21`
  - Evidencia: ops.writeFile(join(targetDir, 'eslint.config.mjs'), configContent); — sin verificar si ya existe eslint.config.js, eslint.config.cjs o .eslintrc.* en el proyecto.
  - Por qué importa: Caso de borde de proyecto preexistente: si el proyecto ya tiene una config flat con otra extensión (eslint.config.js) o legacy (.eslintrc.json), escribir eslint.config.mjs deja dos configuraciones que pueden coexistir o entrar en conflicto según la resolución de ESLint, dejando al usuario con un estado ambiguo y difícil de diagnosticar.

### Validación — ¿es lo que se pedía?
- **[HIGH] El flag --version (documentado en README) reporta 1.0.0, no la versión real 1.0.2 del paquete** (`val-requirements`)
  - `packages/cli/src/index.ts:39,43`
  - Evidencia: program ... .version('1.0.0') y header('devground-init v1.0.0'); en package.json "version": "1.0.2" (package.json:3). El README documenta el flag `-V, --version` (README líneas 18) como forma de obtener la versión.
  - Por qué importa: El README promete que `-V, --version` entrega la versión del CLI. El código tiene la versión hardcodeada a '1.0.0' en dos lugares y nunca lee package.json, así que devuelve un valor desincronizado y falso (la real es 1.0.2). Cualquier usuario o script que dependa de `devground-init --version` para verificar qué versión está corriendo recibe información incorrecta. Es un claim incumplido + bug de mantenimiento (la versión debería leerse de package.json, no duplicarse).
- **[HIGH] --version reporta 1.0.0 pero el paquete publicado es 1.0.2 (versión hardcodeada y desalineada)** (`val-contracts`)
  - `/Users/macbookpro/Documents/devground-1/packages/cli/src/index.ts:39`
  - Evidencia: .version('1.0.0') en index.ts mientras package.json declara "version": "1.0.2". El número está hardcodeado, no se lee de package.json.
  - Por qué importa: El contrato externo del CLI es deshonesto: `devground-init --version` (flag documentado en README) responde 1.0.0, una versión que no corresponde a la realmente instalada (1.0.2 segun package.json y CHANGELOG). Cualquier usuario, script de CI o reporte de bug que dependa de la versión recibe información falsa. Ademas, al estar hardcodeado en lugar de leerse de package.json, cada bump de version (ya van 3: 1.0.0→1.0.1→1.0.2) deja la cifra desactualizada de forma silenciosa, perpetuando el desfase.
- **[MEDIUM] El README omite por completo el instalador 'Architecture guide + ADR templates' que el CLI sí instala** (`val-requirements`)
  - `packages/cli/src/index.ts:31`
  - Evidencia: ALL_INSTALLERS incluye { name: 'Architecture guide + ADR templates', value: 'architecture-guide', install: architectureGuide.install }, que ejecuta `npx devground-architecture` e instala `@devground/architecture-guide` (architecture-guide.ts:9-12). El README 'What it does' solo lista 7 herramientas (Prettier, ESLint, TypeScript, Commitlint, lint-staged, Husky, AGENTS.md) — README líneas 27-35.
  - Por qué importa: El README presenta su tabla como la lista completa de lo que el CLI instala ('Installs and configures each selected tool'). El comportamiento real instala una 8a herramienta no documentada que escribe en `knowledge/` y agrega una dependencia. Un usuario que corra `--yes` o `--preset full` obtiene artefactos (carpeta knowledge/, dependencia architecture-guide) que el README nunca menciona. Documentación desactualizada respecto al comportamiento real.
- **[MEDIUM] Los instaladores sobrescriben archivos de configuración existentes sin verificar ni avisar, pese a presentarse como scaffolder 'into any project'** (`val-requirements`)
  - `packages/cli/src/installers/ops.ts:20`
  - Evidencia: writeFile: (path, content) => writeFileSync(path, content, 'utf-8') — sin existsSync previo. tsconfig.ts:30-33 y :51-54 y :64-67 escriben tsconfig.json/tsconfig.typecheck.json incondicionalmente; eslint.ts:21 escribe eslint.config.mjs; commitlint.ts:13 escribe commitlint.config.js. Ninguno chequea si el archivo ya existe.
  - Por qué importa: El README dice 'Scaffold @devground development standards into ANY project' y 'npx devground-init' sobre proyectos existentes. En un proyecto que ya tiene tsconfig.json/eslint.config.mjs/commitlint.config.js, el CLI los reemplaza por completo de forma silenciosa (sin prompt de confirmación ni backup), perdiendo configuración previa del usuario. El README no advierte de este comportamiento destructivo, generando una expectativa razonable de que un 'init' es aditivo y seguro. Es un incumplimiento de la promesa implícita de seguridad de un scaffolder sobre proyectos existentes.
- **[MEDIUM] Banner de cabecera imprime 'v1.0.0' hardcodeado, duplicando la mentira de versión** (`val-contracts`)
  - `/Users/macbookpro/Documents/devground-1/packages/cli/src/index.ts:43`
  - Evidencia: header('devground-init v1.0.0'); — segundo lugar donde el número de versión está escrito a mano, también desalineado con package.json (1.0.2).
  - Por qué importa: Aparte de --version, la salida normal del comando muestra a todo usuario un banner con una versión incorrecta en cada ejecución. Es una segunda fuente de verdad hardcodeada que también quedó atrás respecto a package.json. La forma de la salida (lo que el usuario ve) afirma ser una versión que no es. Ambos literales deberían derivar de un único punto: la versión real del package.json.
- **[MEDIUM] --preset acepta valores inválidos en silencio sin error ni validación** (`val-contracts`)
  - `/Users/macbookpro/Documents/devground-1/packages/cli/src/index.ts:40-68`
  - Evidencia: .option('--preset <name>', 'Install preset (full | agents-only)') solo compara opts.preset === 'full' y opts.preset === 'agents-only'; cualquier otro valor (ej. --preset foo o un typo como --preset agents) cae al else y abre el prompt interactivo en lugar de fallar.
  - Por qué importa: El contrato del flag declara dos valores válidos (full | agents-only), pero un valor desconocido no produce error ni advertencia: el CLI ignora el flag y entra en modo interactivo. En un entorno no interactivo (CI, --preset mal escrito) esto degrada silenciosamente a un prompt que se cuelga o, peor, el usuario cree haber aplicado un preset que nunca se aplicó. Un contrato honesto debería rechazar valores fuera del enum con exit code distinto de 0.
- **[LOW] El header impreso clava 'v1.0.0' en cada ejecución, contradiciendo la versión publicada** (`val-requirements`)
  - `packages/cli/src/index.ts:43`
  - Evidencia: header('devground-init v1.0.0'); mientras package.json declara version 1.0.2 (package.json:3) y CHANGELOG.md documenta hasta 1.0.2.
  - Por qué importa: Salida visible al usuario que afirma una versión incorrecta del CLI en cada corrida. No rompe funcionalidad pero comunica un dato falso y delata duplicación de la versión en código en lugar de derivarla de package.json (misma causa raíz que el bug de --version).

### Auditoría — ¿está bien construida?
- **[HIGH] La orquestación del CLI (index.ts) no tiene ningún test: selección por preset, flag --yes, exit codes y aislamiento de errores quedan sin cubrir** (`aud-tests`)
  - `/Users/macbookpro/Documents/devground-1/packages/cli/src/index.ts:42-109`
  - Evidencia: El grep `grep -rn "preset|opts.yes|ALL_INSTALLERS|process.exit|catch" src/**/*.test.ts` devuelve "no references to CLI orchestration in tests". Toda la lógica de `.action(async (opts) => {...})` —incluyendo `if (opts.yes || opts.preset === 'full') selectedInstallers = ALL_INSTALLERS;`, el filtro `=== 'agents-only'`, el `process.exit(1)` al fallar detectStack, el `process.exit(0)` cuando no se selecciona nada, y el bucle `for (const installer of selectedInstallers) { try { installer.install(options) } catch ... }`— no es ejercida por ningún .test.ts.
  - Por qué importa: index.ts contiene la lógica de decisión MÁS crítica del CLI: qué se instala según el preset/flag y la garantía de que un instalador que falla no aborta los demás (try/catch por instalador, línea 96-103). Si alguien rompe el mapeo de presets (ej. 'full' deja de seleccionar todo, o 'agents-only' filtra mal), ningún test lo detecta. Tampoco se verifica que el preset desconocido caiga al flujo interactivo, ni que el fallo de un instalador no propague. Es el corazón del comportamiento observable del usuario y está 100% sin red de seguridad.
- **[HIGH] El aislamiento de fallos entre instaladores (un install() que lanza no debe abortar el resto) no está testeado** (`aud-tests`)
  - `/Users/macbookpro/Documents/devground-1/packages/cli/src/index.ts:96-103`
  - Evidencia: El bucle envuelve cada `installer.install(options)` en try/catch y reporta `error(\`Failed to install ${installer.name}: ${message}\`)` para continuar con el siguiente. installers.test.ts solo prueba cada instalador en aislamiento por su happy path (ej. `prettier.install(optionsFor(NODE_STACK, ops))`), nunca el escenario en que uno arroja y los posteriores deben ejecutarse igual.
  - Por qué importa: El propósito explícito de ese try/catch es la resiliencia: si husky falla (ej. `npx devground-husky` no existe), prettier/eslint ya aplicados no se pierden y los siguientes se intentan. Esa garantía de comportamiento crítico (instalación parcial tolerante a fallos) nunca se ejercita; un refactor que mueva el try/catch fuera del bucle convertiría un fallo aislado en un abort total sin que ningún test rojo lo avise.
- **[MEDIUM] Cada instalador dispara su propia invocación del gestor de paquetes; con todo seleccionado son hasta 7 instalaciones secuenciales en vez de una** (`aud-perf`)
  - `packages/cli/src/utils/package-json.ts:41-44`
  - Evidencia: export function addDevDependency(dir, pm, ...packages) { const cmd = `${getInstallCmd(pm)} ${packages.join(' ')}`; run(cmd, dir); } — y cada installer (prettier.ts:9, eslint.ts:15, tsconfig.ts:11, commitlint.ts:10, lint-staged.ts:9, husky.ts:9, agents-md.ts:9, architecture-guide.ts:9) llama addDevDependency por separado dentro del bucle `for (const installer of selectedInstallers) installer.install(options)` (index.ts:96-103).
  - Por qué importa: run() usa execSync (utils/exec.ts:8), que bloquea el proceso. Al seleccionar el preset 'full'/-y (los 8 instaladores), se ejecutan hasta 7 procesos `npm install -D` / `pnpm add -D` / `yarn add -D` en serie, cada uno con su propia resolución completa del árbol de dependencias, reescritura de lockfile y posible acceso a red. Es trabajo redundante: resolver e instalar el grafo N veces en lugar de agregar todos los paquetes en una sola invocación del gestor (que resuelve una vez). En proyectos reales esto se traduce en segundos extra de espera por cada paso. Batchear los paquetes en una única llamada de instalación al final (recolectando el set de paquetes de los instaladores seleccionados) eliminaría las pasadas de resolución repetidas. Es el único costo de rendimiento de verdad relevante en este CLI; el I/O síncrono restante es aceptable para un scaffolder de un solo disparo.
- **[MEDIUM] La construcción del comando de instalación por package manager (getInstallCmd / addDevDependency) nunca se ejercita: los tests usan un fake que se salta esa lógica** (`aud-tests`)
  - `/Users/macbookpro/Documents/devground-1/packages/cli/src/utils/package-json.ts:27-44`
  - Evidencia: `getInstallCmd` mapea pnpm→'pnpm add -D', yarn→'yarn add -D', npm→'npm install -D' y `addDevDependency` arma `${getInstallCmd(pm)} ${packages.join(' ')}`. En installers.test.ts el fake `makeRecordingOps` reemplaza `addDevDependency` por `(dir, pm, ...packages) => devDeps.push(...)` (líneas 55-57), por lo que las aserciones solo comprueban el PM propagado y la lista de paquetes, nunca el string de comando real. package-json.test.ts solo cubre read/writePackageJson, no addDevDependency.
  - Por qué importa: El comando shell que efectivamente se ejecuta contra el sistema del usuario es comportamiento crítico: un error en el flag (`-D` vs `--save-dev`, o `yarn add` sin `-D`) instalaría dependencias en el lugar equivocado o fallaría. Como toda la suite mockea esta función, la traducción PackageManager→comando no tiene una sola aserción sobre su salida; está completamente fuera de cobertura.
- **[LOW] addDevDependency construye el comando concatenando nombres de paquete con join(' ')** (`aud-security`)
  - `/Users/macbookpro/Documents/devground-1/packages/cli/src/utils/package-json.ts:41-44`
  - Evidencia: const cmd = `${getInstallCmd(pm)} ${packages.join(' ')}`; run(cmd, dir);
  - Por qué importa: Los nombres de paquete se interpolan crudos en un string que termina en execSync (shell). En el código actual TODOS los argumentos `packages` son literales hardcodeados en los installers (p.ej. '@devground/prettier-config', 'eslint', 'eslint-config-next' en eslint.ts:12), nunca provienen de package.json del proyecto objetivo ni de input del usuario, así que no es explotable hoy. Se reporta como deuda de diseño: la firma variádica `...packages: string[]` sobre un comando de shell es frágil ante futuros usos con datos no confiables. Preferible execFileSync(getInstallCmd, packages) sin shell.
- **[LOW] El callback .action() de Commander en index.ts concentra varias responsabilidades en una sola función anónima de ~67 líneas** (`aud-limits`)
  - `/Users/macbookpro/Documents/devground-1/packages/cli/src/index.ts:42-109`
  - Evidencia: .action(async (opts) => { ... }) abarca: header/setup (43-45), detección de stack con try/catch (47-54), impresión de info (56-59), tres ramas de selección de instaladores (opts.yes/preset 'full' -> 65; preset 'agents-only' -> 67; prompt interactivo con validacion de respuesta vacia -> 69-91) y el bucle de instalacion con manejo de error por instalador (96-103).
  - Por qué importa: Es la unica funcion del paquete que mezcla orquestacion, I/O interactivo (prompts), deteccion y ejecucion en un mismo cierre anonimo. Su complejidad ciclomatica (multiples if/else + bucle + try/catch anidados) y su tamano la hacen mas dificil de testear de forma aislada que el resto del codigo, que esta bien descompuesto en funciones puras pequenas. Extraer la logica de seleccion (resolveSelectedInstallers) y el bucle de instalacion (runInstallers) a funciones con nombre reduciria el cierre a orquestacion legible. Severidad baja: el modulo total es de 111 lineas y ninguna metrica supera umbrales graves; es deuda de mantenibilidad, no un defecto funcional.
- **[LOW] utils/exec.ts (run con execSync, el único punto que ejecuta shell real) no tiene test** (`aud-tests`)
  - `/Users/macbookpro/Documents/devground-1/packages/cli/src/utils/exec.ts:7-9`
  - Evidencia: `run` hace `execSync(cmd, { cwd, encoding: 'utf-8', stdio: 'pipe' }).trim()` y documenta "Throws on non-zero exit code". No existe `exec.test.ts` (find solo lista detect-stack.test.ts, package-json.test.ts e installers.test.ts) y en installers.test.ts `run` está mockeado a `() => ''`.
  - Por qué importa: Es el límite con el sistema operativo y el contrato (trim de stdout, propagación de error en exit≠0) del que dependen husky/agents-md/architecture-guide. Aunque es una capa fina, su comportamiento de error es lo que el try/catch de index.ts asume; no está verificado que efectivamente lance ni que recorte stdout. Severidad baja por ser un wrapper delgado, pero sigue siendo una ruta crítica sin cobertura.

## Descartados por el filtro adversarial (transparencia)
- ~~detectFramework clasifica como 'node' cualquier package.json con al menos una dependencia, incluso sin runtime real~~ (`qa-edge`, info) — refutado 2/2. Razón: El hallazgo es severidad "info" y, por su propia redacción, describe un comportamiento "aceptable" — no afirma un bug. La parte mecánica del código se confirma línea por línea en detect-stack.ts:23-28: tras chequear 'next' (L24) y 'react' (L25), cualquier allDeps no vacío retorna 'node' (L26) y un p
- ~~Errores de comandos externos (npx/pnpm add) pierden el stderr del proceso hijo, mensaje no accionable~~ (`qa-errors`, medium) — refutado 1/2. Razón: El hallazgo afirma que con `stdio: 'pipe'` el stderr del hijo "queda en err.stderr/err.stdout" y "nunca llega al usuario" porque el catch de index.ts:100 usa solo `err.message`. Esto es factualmente incorrecto para `execSync` de Node. Verifiqué empíricamente en el runtime (Node v26) que `execSync` c
- ~~Cancelación del usuario (Ctrl+C) se trata como 'nada seleccionado' con exit 0 y sin onCancel~~ (`qa-errors`, low) — refutado 1/2. Razón: El hallazgo describe correctamente la mecánica de `prompts` en /Users/macbookpro/Documents/devground-1/packages/cli/src/index.ts:70-85: al cancelar (Ctrl+C/ESC) la librería devuelve un objeto vacío, no hay `onCancel`, y `response.tools` cae en `if (!response.tools || response.tools.length === 0) { i
- ~~Utilidad run() ejecuta vía shell (execSync) con string concatenado, sin sanitización~~ (`aud-security`, low) — refutado 1/2. Razón: El código citado es verbatim y correcto: `run()` en /Users/macbookpro/Documents/devground-1/packages/cli/src/utils/exec.ts:7-9 usa `execSync`, que efectivamente pasa por `/bin/sh -c`. Sin embargo, el hallazgo se refuta porque NO aplica como vulnerabilidad real en este flujo: no existe ningún vector 
- ~~package.json se lee, parsea y reescribe de forma independiente en cada instalador que lo muta, en vez de leer/escribir una sola vez~~ (`aud-perf`, low) — refutado 1/2. Razón: El hallazgo es un falso positivo porque malinterpreta el flujo y su recomendación ("una sola lectura/escritura coordinada") introduciría justamente el bug de pisar cambios que dice temer.

Evidencia clave: en cada instalador el orden es addDevDependency PRIMERO, luego readPackageJson, luego writePac
- ~~JSON.parse del package.json externo se castea a Record<string, unknown> sin validar (frontera de parsing)~~ (`aud-types`, medium) — refutado 2/2. Razón: El code-reading literal es correcto: en /Users/macbookpro/Documents/devground-1/packages/cli/src/utils/package-json.ts:12 existe `return JSON.parse(raw) as Record<string, unknown>;` sin validación de runtime. Pero el hallazgo se REFUTA por dos razones de fondo:

1) MISREPRESENTA EL ADR. El hallazgo 
- ~~Las dependencias del package.json se afirman como Record<string, string> sin verificar la forma real~~ (`aud-types`, medium) — refutado 1/2. Razón: El hallazgo es FACTUALMENTE correcto en su núcleo: en `detect-stack.ts:12-13` se afirma `as Record<string, string>` sobre `pkg.dependencies`/`pkg.devDependencies` sin type-guard, y `readPackageJson` (package-json.ts:9-13) devuelve `Record<string, unknown>` desde `JSON.parse` sin validar la forma. No
- ~~El retorno de la libreria prompts se castea a string[] sin validacion (frontera de input de usuario)~~ (`aud-types`, low) — refutado 1/2. Razón: El hallazgo es un falso positivo en cuanto a su fundamento (cita ADR-0011) y exagera el riesgo. Hechos verificados en el código:

1. TIPO REAL DEL ORIGEN: `@types/prompts` define `Answers<T> = { [id in T]: any }` (node_modules/.pnpm/@types+prompts@2.4.9/.../index.d.ts:96). Por tanto `response.tools`
